name: Force Copy Pixel Forge Source

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  copy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: main

      - name: Mirror source
        run: |
          rm -rf /tmp/source
          git clone --depth 1 https://github.com/ViperXC132/pixel-forge-client.git /tmp/source
          rm -rf /tmp/source/.git
          rm -f /tmp/source/.github/workflows/build.yml
          cp -a /tmp/source/. .

      - name: Force push mirror
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "Mirror pixel-forge-client source"
          git push --force origin HEAD:main
